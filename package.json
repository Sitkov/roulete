{
  "name": "videochat-roulette-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Full video chat roulette app with admin, premium, PWA-ready frontend and Node.js signaling backend.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,blue \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "start": "concurrently -n backend,frontend -c green,blue \"npm --workspace backend start\" \"npm --workspace frontend start\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}



